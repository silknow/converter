SELECT  DISTINCT ?obj ?pred
WHERE {
  
                ?statement a rdf:Statement .
                ?statement rdf:subject ?prod .
                ?statement rdf:predicate ?pred .
                ?prod ecrm:P108_has_produced ?obj 
}
ORDER BY ?obj
